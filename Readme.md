# fio-csv

The script `fio-csv.sh` runs bunch of tests to check storage performance and save results to a `*.csv` file

## Run

```bash
./fio-csv.sh                # Uses current directory
./fio-csv.sh /mnt/disk      # Specify test directory
```